source "https://rubygems.org"

gem "jekyll", "~> 4.4.1"
gem "webrick", "~> 1.9"

# Cloudflare omits test gems during install. Keep them optional at build time too.
group :test, optional: true do
  gem "minitest", "~> 5.25"
end
